<h1>Students</h1>

<ul>
  <% @students.each do |student| %>
    <li>
        <a href="<%= student_path(student) %>">
            <%= student.name %> <%= student.surname %>
        </a>
    </li>
  <% end %>
</ul>

<a href="<%= new_student_path() %>">New student</a>